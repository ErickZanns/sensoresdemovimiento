import { Routes } from '@angular/router';
import { StepCounterComponent } from './components/step-counter/step-counter.component';

export const appRoutes: Routes = [
  { path: '', component: StepCounterComponent } // Ruta principal
];
